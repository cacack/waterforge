// Public API for the Waterforge engine (chem + solver).
//
// The framework-agnostic solver engine lands in #7; this file is its export
// surface. Keeping it here lets the UI import from `$lib` without reaching into
// engine internals.
export {}
